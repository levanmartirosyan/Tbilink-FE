import { Component } from '@angular/core';
import { Nav } from '../../shared/nav/nav';

@Component({
  selector: 'app-feed',
  imports: [Nav],
  templateUrl: './feed.html',
  styleUrl: './feed.scss',
})
export class Feed {}
